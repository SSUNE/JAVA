p.348

◼ ︎지정접근자
1. 클래스 : DTO(Data Transfer Object) / DAO(Data Access Object)
    DTO : 데이터를 전달목적으로 설계된 클래스
    DAO : 데이터(DB) 접근을 목적으로 설계된 클래스
        - CRUD CREATE(insert), Read(select), Update(update), Delete(delete)
    Interface : 설계도
    Abstract : 설계도 + 클래스

2. 접근제어자 종류
    public : 누구나 접근가능, 공개
    protected : 같은 패키지(폴더)에 있는 객체와 상속관계의 객체들만 접근
    package : 같은 패키지(폴더)에 있는 객체, 생략하면 기본값
    private : 해당 클래스내에서만 접근 가능, 비공개

3. 접근제어자의 사용범위
             클래스내부 동일패키지 하위클래스 그외
public       o       o       o       o
protected    o       o       o       x
package      o       o       x       x
private      o       x       x       x

[ public > protected > package > private ]

4. getter, setter
    - private 속성으오 멤버변수를 각각 개별적으로 접근가능하게 만들어줌.

